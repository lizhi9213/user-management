const config = {
  // 启动端口
  port: 3000,

  // 数据库配置
  database: {
    DATABASE: 'userslistsql',
    USERNAME: 'root',
    PASSWORD: 'lizhi9213',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config

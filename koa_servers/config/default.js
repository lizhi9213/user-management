const config = {
  // Start the port
  port: 3000,

  // Database configuration
  database: {
    DATABASE: 'userslistsql',
    USERNAME: 'root',
    PASSWORD: 'lizhi9213',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config
